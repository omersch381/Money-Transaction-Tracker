pragma solidity >=0.4.22 <0.7.0;

contract Utils {
    function getName (address[] memory addresses, address friend) public returns (string memory) {
        // for loop which will find the friend's name
        // in the array and will return it
    }
}